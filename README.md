# coding-project-template

On the Terminal

````git clone ...repo-url

Write the command to enter the custom_hook folder in the terminal. Use the below command to navigate to the custom_hook folder in the terminal.
```cd custom_hook

Write the following command in the terminal and hit Enter to install all the necessary packages to execute the applicatio
```npm install

Then execute the following command to run the application
```npm run preview
````
